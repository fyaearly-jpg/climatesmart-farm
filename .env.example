# Salin file ini menjadi .env.local (lokal) atau isi di Vercel:
# Project > Settings > Environment Variables.
#
# Kunci penandatangan sesi login (rahasia, HANYA di server — tanpa awalan NEXT_PUBLIC_).
# Minimal 32 karakter. Buat dengan:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Di development lokal boleh dikosongkan (ada kunci bawaan khusus dev),
# tetapi di production WAJIB diisi.
SESSION_SECRET=
